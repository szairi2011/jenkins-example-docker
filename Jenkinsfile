pipeline {
    agent {
        label 'ubuntu-docker-maven'
    }
    stages {
        stage('Hello') {
            steps {
                echo 'Hello from jenkins-example-docker Jenkinsfile'
            }
        }
    }
}
